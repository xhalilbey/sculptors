/**
 * Types shared across the app. Only what live code imports through
 * `@/types` is re-exported here; error classes live in `@/types/errors`.
 */
export type { NavItem, NavigationGroup } from './navigation';
